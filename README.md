
# Оптимизация и работа с массивами в PostgreSQL (DVD Rental DB)

Проект демонстрирует навыки написания сложных SQL-запросов, использования CTE и анализа производительности.

## 🚀 Что было сделано:
- **Работа с массивами:** Реализовано 3 способа поиска в массивах (`@>`, `ANY`, `UNNEST`).
- **Анализ производительности:** Проведено профилирование запросов с помощью `EXPLAIN ANALYZE`.
- **Архитектура:** Использование CTE (Common Table Expressions) и подзапросов для агрегации данных.
- **Оптимизация доступа:** Создание материализованных представлений (`MATERIALIZED VIEW`).

## 📊 Результаты анализа (Performance Review):
При сравнении CTE и подзапросов для данной БД стоимость составила **693.86** в обоих случаях. Это связано с тем, что современный планировщик PostgreSQL (начиная с версии 12) умеет оптимизировать CTE так же эффективно, как и подзапросы.

Самый дешевый поиск в массиве: оператор `@>` (cost 67.50).

